// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: controls_settings.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "scp.webui.v1";

/** Identifies which controls settings group the client wants to fetch. */
export enum ControlsSettingRequestType {
  CONTROLS_SETTING_MOUSE_SENSITIVITY = 0,
  CONTROLS_SETTING_MOVEMENT = 1,
  CONTROLS_SETTING_CHARACTER = 2,
  UNRECOGNIZED = -1,
}

export function controlsSettingRequestTypeFromJSON(object: any): ControlsSettingRequestType {
  switch (object) {
    case 0:
    case "CONTROLS_SETTING_MOUSE_SENSITIVITY":
      return ControlsSettingRequestType.CONTROLS_SETTING_MOUSE_SENSITIVITY;
    case 1:
    case "CONTROLS_SETTING_MOVEMENT":
      return ControlsSettingRequestType.CONTROLS_SETTING_MOVEMENT;
    case 2:
    case "CONTROLS_SETTING_CHARACTER":
      return ControlsSettingRequestType.CONTROLS_SETTING_CHARACTER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ControlsSettingRequestType.UNRECOGNIZED;
  }
}

export function controlsSettingRequestTypeToJSON(object: ControlsSettingRequestType): string {
  switch (object) {
    case ControlsSettingRequestType.CONTROLS_SETTING_MOUSE_SENSITIVITY:
      return "CONTROLS_SETTING_MOUSE_SENSITIVITY";
    case ControlsSettingRequestType.CONTROLS_SETTING_MOVEMENT:
      return "CONTROLS_SETTING_MOVEMENT";
    case ControlsSettingRequestType.CONTROLS_SETTING_CHARACTER:
      return "CONTROLS_SETTING_CHARACTER";
    case ControlsSettingRequestType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Identifies a specific controls checkbox toggle to update. */
export enum ControlsCheckboxType {
  CONTROLS_INVERT_Y = 0,
  CONTROLS_HOLD_SPRINT = 1,
  CONTROLS_HOLD_CROUCH = 2,
  CONTROLS_HOLD_TO_SELECT_ITEM = 3,
  CONTROLS_AUTO_SWITCH = 4,
  CONTROLS_HOLD_INVENTORY = 5,
  UNRECOGNIZED = -1,
}

export function controlsCheckboxTypeFromJSON(object: any): ControlsCheckboxType {
  switch (object) {
    case 0:
    case "CONTROLS_INVERT_Y":
      return ControlsCheckboxType.CONTROLS_INVERT_Y;
    case 1:
    case "CONTROLS_HOLD_SPRINT":
      return ControlsCheckboxType.CONTROLS_HOLD_SPRINT;
    case 2:
    case "CONTROLS_HOLD_CROUCH":
      return ControlsCheckboxType.CONTROLS_HOLD_CROUCH;
    case 3:
    case "CONTROLS_HOLD_TO_SELECT_ITEM":
      return ControlsCheckboxType.CONTROLS_HOLD_TO_SELECT_ITEM;
    case 4:
    case "CONTROLS_AUTO_SWITCH":
      return ControlsCheckboxType.CONTROLS_AUTO_SWITCH;
    case 5:
    case "CONTROLS_HOLD_INVENTORY":
      return ControlsCheckboxType.CONTROLS_HOLD_INVENTORY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ControlsCheckboxType.UNRECOGNIZED;
  }
}

export function controlsCheckboxTypeToJSON(object: ControlsCheckboxType): string {
  switch (object) {
    case ControlsCheckboxType.CONTROLS_INVERT_Y:
      return "CONTROLS_INVERT_Y";
    case ControlsCheckboxType.CONTROLS_HOLD_SPRINT:
      return "CONTROLS_HOLD_SPRINT";
    case ControlsCheckboxType.CONTROLS_HOLD_CROUCH:
      return "CONTROLS_HOLD_CROUCH";
    case ControlsCheckboxType.CONTROLS_HOLD_TO_SELECT_ITEM:
      return "CONTROLS_HOLD_TO_SELECT_ITEM";
    case ControlsCheckboxType.CONTROLS_AUTO_SWITCH:
      return "CONTROLS_AUTO_SWITCH";
    case ControlsCheckboxType.CONTROLS_HOLD_INVENTORY:
      return "CONTROLS_HOLD_INVENTORY";
    case ControlsCheckboxType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Current mouse sensitivity slider value. */
export interface MouseSensitivityResponse {
  sensitivity: number;
}

/** Updates the mouse sensitivity slider value. */
export interface MouseSensitivitySetRequest {
  sensitivity: number;
}

/** Movement-related toggle states. */
export interface ControlsMovementResponse {
  invertY: boolean;
  holdSprint: boolean;
  holdCrouch: boolean;
}

/** Character and inventory interaction toggle states. */
export interface ControlsCharacterResponse {
  holdToSelectItem: boolean;
  autoSwitch: boolean;
  holdInventory: boolean;
}

/** Sets a single controls checkbox state. */
export interface ControlCheckboxSetRequest {
  controlType: ControlsCheckboxType;
  checkboxValue: boolean;
}

/** Requests one controls settings section from the backend. */
export interface RequestGetControlSettings {
  requestType: ControlsSettingRequestType;
}

/** Applies a controls settings change. Only one payload variant is valid per request. */
export interface RequestSetControlSettings {
  sensitivity?: MouseSensitivitySetRequest | undefined;
  checkbox?: ControlCheckboxSetRequest | undefined;
}

/** Returns the controls settings payload matching the requested section. */
export interface ResponseControlSettings {
  requestedType: ControlsSettingRequestType;
  sensitivity?: MouseSensitivityResponse | undefined;
  movement?: ControlsMovementResponse | undefined;
  character?: ControlsCharacterResponse | undefined;
}

function createBaseMouseSensitivityResponse(): MouseSensitivityResponse {
  return { sensitivity: 0 };
}

export const MouseSensitivityResponse: MessageFns<MouseSensitivityResponse> = {
  encode(message: MouseSensitivityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sensitivity !== 0) {
      writer.uint32(13).float(message.sensitivity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MouseSensitivityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMouseSensitivityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.sensitivity = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MouseSensitivityResponse {
    return { sensitivity: isSet(object.sensitivity) ? globalThis.Number(object.sensitivity) : 0 };
  },

  toJSON(message: MouseSensitivityResponse): unknown {
    const obj: any = {};
    if (message.sensitivity !== 0) {
      obj.sensitivity = message.sensitivity;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MouseSensitivityResponse>, I>>(base?: I): MouseSensitivityResponse {
    return MouseSensitivityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MouseSensitivityResponse>, I>>(object: I): MouseSensitivityResponse {
    const message = createBaseMouseSensitivityResponse();
    message.sensitivity = object.sensitivity ?? 0;
    return message;
  },
};

function createBaseMouseSensitivitySetRequest(): MouseSensitivitySetRequest {
  return { sensitivity: 0 };
}

export const MouseSensitivitySetRequest: MessageFns<MouseSensitivitySetRequest> = {
  encode(message: MouseSensitivitySetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sensitivity !== 0) {
      writer.uint32(13).float(message.sensitivity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MouseSensitivitySetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMouseSensitivitySetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.sensitivity = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MouseSensitivitySetRequest {
    return { sensitivity: isSet(object.sensitivity) ? globalThis.Number(object.sensitivity) : 0 };
  },

  toJSON(message: MouseSensitivitySetRequest): unknown {
    const obj: any = {};
    if (message.sensitivity !== 0) {
      obj.sensitivity = message.sensitivity;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MouseSensitivitySetRequest>, I>>(base?: I): MouseSensitivitySetRequest {
    return MouseSensitivitySetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MouseSensitivitySetRequest>, I>>(object: I): MouseSensitivitySetRequest {
    const message = createBaseMouseSensitivitySetRequest();
    message.sensitivity = object.sensitivity ?? 0;
    return message;
  },
};

function createBaseControlsMovementResponse(): ControlsMovementResponse {
  return { invertY: false, holdSprint: false, holdCrouch: false };
}

export const ControlsMovementResponse: MessageFns<ControlsMovementResponse> = {
  encode(message: ControlsMovementResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invertY !== false) {
      writer.uint32(8).bool(message.invertY);
    }
    if (message.holdSprint !== false) {
      writer.uint32(16).bool(message.holdSprint);
    }
    if (message.holdCrouch !== false) {
      writer.uint32(24).bool(message.holdCrouch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlsMovementResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlsMovementResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.invertY = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.holdSprint = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.holdCrouch = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ControlsMovementResponse {
    return {
      invertY: isSet(object.invertY)
        ? globalThis.Boolean(object.invertY)
        : isSet(object.invert_y)
        ? globalThis.Boolean(object.invert_y)
        : false,
      holdSprint: isSet(object.holdSprint)
        ? globalThis.Boolean(object.holdSprint)
        : isSet(object.hold_sprint)
        ? globalThis.Boolean(object.hold_sprint)
        : false,
      holdCrouch: isSet(object.holdCrouch)
        ? globalThis.Boolean(object.holdCrouch)
        : isSet(object.hold_crouch)
        ? globalThis.Boolean(object.hold_crouch)
        : false,
    };
  },

  toJSON(message: ControlsMovementResponse): unknown {
    const obj: any = {};
    if (message.invertY !== false) {
      obj.invertY = message.invertY;
    }
    if (message.holdSprint !== false) {
      obj.holdSprint = message.holdSprint;
    }
    if (message.holdCrouch !== false) {
      obj.holdCrouch = message.holdCrouch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ControlsMovementResponse>, I>>(base?: I): ControlsMovementResponse {
    return ControlsMovementResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlsMovementResponse>, I>>(object: I): ControlsMovementResponse {
    const message = createBaseControlsMovementResponse();
    message.invertY = object.invertY ?? false;
    message.holdSprint = object.holdSprint ?? false;
    message.holdCrouch = object.holdCrouch ?? false;
    return message;
  },
};

function createBaseControlsCharacterResponse(): ControlsCharacterResponse {
  return { holdToSelectItem: false, autoSwitch: false, holdInventory: false };
}

export const ControlsCharacterResponse: MessageFns<ControlsCharacterResponse> = {
  encode(message: ControlsCharacterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.holdToSelectItem !== false) {
      writer.uint32(8).bool(message.holdToSelectItem);
    }
    if (message.autoSwitch !== false) {
      writer.uint32(16).bool(message.autoSwitch);
    }
    if (message.holdInventory !== false) {
      writer.uint32(24).bool(message.holdInventory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlsCharacterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlsCharacterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.holdToSelectItem = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.autoSwitch = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.holdInventory = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ControlsCharacterResponse {
    return {
      holdToSelectItem: isSet(object.holdToSelectItem)
        ? globalThis.Boolean(object.holdToSelectItem)
        : isSet(object.hold_to_select_item)
        ? globalThis.Boolean(object.hold_to_select_item)
        : false,
      autoSwitch: isSet(object.autoSwitch)
        ? globalThis.Boolean(object.autoSwitch)
        : isSet(object.auto_switch)
        ? globalThis.Boolean(object.auto_switch)
        : false,
      holdInventory: isSet(object.holdInventory)
        ? globalThis.Boolean(object.holdInventory)
        : isSet(object.hold_inventory)
        ? globalThis.Boolean(object.hold_inventory)
        : false,
    };
  },

  toJSON(message: ControlsCharacterResponse): unknown {
    const obj: any = {};
    if (message.holdToSelectItem !== false) {
      obj.holdToSelectItem = message.holdToSelectItem;
    }
    if (message.autoSwitch !== false) {
      obj.autoSwitch = message.autoSwitch;
    }
    if (message.holdInventory !== false) {
      obj.holdInventory = message.holdInventory;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ControlsCharacterResponse>, I>>(base?: I): ControlsCharacterResponse {
    return ControlsCharacterResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlsCharacterResponse>, I>>(object: I): ControlsCharacterResponse {
    const message = createBaseControlsCharacterResponse();
    message.holdToSelectItem = object.holdToSelectItem ?? false;
    message.autoSwitch = object.autoSwitch ?? false;
    message.holdInventory = object.holdInventory ?? false;
    return message;
  },
};

function createBaseControlCheckboxSetRequest(): ControlCheckboxSetRequest {
  return { controlType: 0, checkboxValue: false };
}

export const ControlCheckboxSetRequest: MessageFns<ControlCheckboxSetRequest> = {
  encode(message: ControlCheckboxSetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.controlType !== 0) {
      writer.uint32(8).int32(message.controlType);
    }
    if (message.checkboxValue !== false) {
      writer.uint32(16).bool(message.checkboxValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlCheckboxSetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlCheckboxSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.controlType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.checkboxValue = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ControlCheckboxSetRequest {
    return {
      controlType: isSet(object.controlType)
        ? controlsCheckboxTypeFromJSON(object.controlType)
        : isSet(object.control_type)
        ? controlsCheckboxTypeFromJSON(object.control_type)
        : 0,
      checkboxValue: isSet(object.checkboxValue)
        ? globalThis.Boolean(object.checkboxValue)
        : isSet(object.checkbox_value)
        ? globalThis.Boolean(object.checkbox_value)
        : false,
    };
  },

  toJSON(message: ControlCheckboxSetRequest): unknown {
    const obj: any = {};
    if (message.controlType !== 0) {
      obj.controlType = controlsCheckboxTypeToJSON(message.controlType);
    }
    if (message.checkboxValue !== false) {
      obj.checkboxValue = message.checkboxValue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ControlCheckboxSetRequest>, I>>(base?: I): ControlCheckboxSetRequest {
    return ControlCheckboxSetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlCheckboxSetRequest>, I>>(object: I): ControlCheckboxSetRequest {
    const message = createBaseControlCheckboxSetRequest();
    message.controlType = object.controlType ?? 0;
    message.checkboxValue = object.checkboxValue ?? false;
    return message;
  },
};

function createBaseRequestGetControlSettings(): RequestGetControlSettings {
  return { requestType: 0 };
}

export const RequestGetControlSettings: MessageFns<RequestGetControlSettings> = {
  encode(message: RequestGetControlSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestType !== 0) {
      writer.uint32(8).int32(message.requestType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestGetControlSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestGetControlSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requestType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestGetControlSettings {
    return {
      requestType: isSet(object.requestType)
        ? controlsSettingRequestTypeFromJSON(object.requestType)
        : isSet(object.request_type)
        ? controlsSettingRequestTypeFromJSON(object.request_type)
        : 0,
    };
  },

  toJSON(message: RequestGetControlSettings): unknown {
    const obj: any = {};
    if (message.requestType !== 0) {
      obj.requestType = controlsSettingRequestTypeToJSON(message.requestType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestGetControlSettings>, I>>(base?: I): RequestGetControlSettings {
    return RequestGetControlSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestGetControlSettings>, I>>(object: I): RequestGetControlSettings {
    const message = createBaseRequestGetControlSettings();
    message.requestType = object.requestType ?? 0;
    return message;
  },
};

function createBaseRequestSetControlSettings(): RequestSetControlSettings {
  return { sensitivity: undefined, checkbox: undefined };
}

export const RequestSetControlSettings: MessageFns<RequestSetControlSettings> = {
  encode(message: RequestSetControlSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sensitivity !== undefined) {
      MouseSensitivitySetRequest.encode(message.sensitivity, writer.uint32(10).fork()).join();
    }
    if (message.checkbox !== undefined) {
      ControlCheckboxSetRequest.encode(message.checkbox, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestSetControlSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestSetControlSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sensitivity = MouseSensitivitySetRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.checkbox = ControlCheckboxSetRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestSetControlSettings {
    return {
      sensitivity: isSet(object.sensitivity) ? MouseSensitivitySetRequest.fromJSON(object.sensitivity) : undefined,
      checkbox: isSet(object.checkbox) ? ControlCheckboxSetRequest.fromJSON(object.checkbox) : undefined,
    };
  },

  toJSON(message: RequestSetControlSettings): unknown {
    const obj: any = {};
    if (message.sensitivity !== undefined) {
      obj.sensitivity = MouseSensitivitySetRequest.toJSON(message.sensitivity);
    }
    if (message.checkbox !== undefined) {
      obj.checkbox = ControlCheckboxSetRequest.toJSON(message.checkbox);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestSetControlSettings>, I>>(base?: I): RequestSetControlSettings {
    return RequestSetControlSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestSetControlSettings>, I>>(object: I): RequestSetControlSettings {
    const message = createBaseRequestSetControlSettings();
    message.sensitivity = (object.sensitivity !== undefined && object.sensitivity !== null)
      ? MouseSensitivitySetRequest.fromPartial(object.sensitivity)
      : undefined;
    message.checkbox = (object.checkbox !== undefined && object.checkbox !== null)
      ? ControlCheckboxSetRequest.fromPartial(object.checkbox)
      : undefined;
    return message;
  },
};

function createBaseResponseControlSettings(): ResponseControlSettings {
  return { requestedType: 0, sensitivity: undefined, movement: undefined, character: undefined };
}

export const ResponseControlSettings: MessageFns<ResponseControlSettings> = {
  encode(message: ResponseControlSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestedType !== 0) {
      writer.uint32(8).int32(message.requestedType);
    }
    if (message.sensitivity !== undefined) {
      MouseSensitivityResponse.encode(message.sensitivity, writer.uint32(18).fork()).join();
    }
    if (message.movement !== undefined) {
      ControlsMovementResponse.encode(message.movement, writer.uint32(26).fork()).join();
    }
    if (message.character !== undefined) {
      ControlsCharacterResponse.encode(message.character, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseControlSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseControlSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requestedType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sensitivity = MouseSensitivityResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.movement = ControlsMovementResponse.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.character = ControlsCharacterResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResponseControlSettings {
    return {
      requestedType: isSet(object.requestedType)
        ? controlsSettingRequestTypeFromJSON(object.requestedType)
        : isSet(object.requested_type)
        ? controlsSettingRequestTypeFromJSON(object.requested_type)
        : 0,
      sensitivity: isSet(object.sensitivity) ? MouseSensitivityResponse.fromJSON(object.sensitivity) : undefined,
      movement: isSet(object.movement) ? ControlsMovementResponse.fromJSON(object.movement) : undefined,
      character: isSet(object.character) ? ControlsCharacterResponse.fromJSON(object.character) : undefined,
    };
  },

  toJSON(message: ResponseControlSettings): unknown {
    const obj: any = {};
    if (message.requestedType !== 0) {
      obj.requestedType = controlsSettingRequestTypeToJSON(message.requestedType);
    }
    if (message.sensitivity !== undefined) {
      obj.sensitivity = MouseSensitivityResponse.toJSON(message.sensitivity);
    }
    if (message.movement !== undefined) {
      obj.movement = ControlsMovementResponse.toJSON(message.movement);
    }
    if (message.character !== undefined) {
      obj.character = ControlsCharacterResponse.toJSON(message.character);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseControlSettings>, I>>(base?: I): ResponseControlSettings {
    return ResponseControlSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseControlSettings>, I>>(object: I): ResponseControlSettings {
    const message = createBaseResponseControlSettings();
    message.requestedType = object.requestedType ?? 0;
    message.sensitivity = (object.sensitivity !== undefined && object.sensitivity !== null)
      ? MouseSensitivityResponse.fromPartial(object.sensitivity)
      : undefined;
    message.movement = (object.movement !== undefined && object.movement !== null)
      ? ControlsMovementResponse.fromPartial(object.movement)
      : undefined;
    message.character = (object.character !== undefined && object.character !== null)
      ? ControlsCharacterResponse.fromPartial(object.character)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
