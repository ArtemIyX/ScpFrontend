// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.36.0
// source: scp_webui.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "scp.webui.v1";

export enum MessageType {
  MESSAGE_INVALID = 0,
  MESSAGE_PING = 1,
  MESSAGE_PONG = 2,
  UNRECOGNIZED = -1,
}

export function messageTypeFromJSON(object: any): MessageType {
  switch (object) {
    case 0:
    case "MESSAGE_INVALID":
      return MessageType.MESSAGE_INVALID;
    case 1:
    case "MESSAGE_PING":
      return MessageType.MESSAGE_PING;
    case 2:
    case "MESSAGE_PONG":
      return MessageType.MESSAGE_PONG;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MessageType.UNRECOGNIZED;
  }
}

export function messageTypeToJSON(object: MessageType): string {
  switch (object) {
    case MessageType.MESSAGE_INVALID:
      return "MESSAGE_INVALID";
    case MessageType.MESSAGE_PING:
      return "MESSAGE_PING";
    case MessageType.MESSAGE_PONG:
      return "MESSAGE_PONG";
    case MessageType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface PingMessage {
  clientTimeMs: string;
  code: number;
}

export interface PongMessage {
  clientTimeMs: string;
  serverTimeMs: string;
  code: number;
}

export interface ScpEnvelope {
  messageType: MessageType;
  messageBytes: Uint8Array;
}

function createBasePingMessage(): PingMessage {
  return { clientTimeMs: "0", code: 0 };
}

export const PingMessage: MessageFns<PingMessage> = {
  encode(message: PingMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientTimeMs !== "0") {
      writer.uint32(8).uint64(message.clientTimeMs);
    }
    if (message.code !== 0) {
      writer.uint32(16).uint32(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.clientTimeMs = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.code = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingMessage {
    return {
      clientTimeMs: isSet(object.clientTimeMs)
        ? globalThis.String(object.clientTimeMs)
        : isSet(object.client_time_ms)
        ? globalThis.String(object.client_time_ms)
        : "0",
      code: isSet(object.code) ? globalThis.Number(object.code) : 0,
    };
  },

  toJSON(message: PingMessage): unknown {
    const obj: any = {};
    if (message.clientTimeMs !== "0") {
      obj.clientTimeMs = message.clientTimeMs;
    }
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PingMessage>, I>>(base?: I): PingMessage {
    return PingMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PingMessage>, I>>(object: I): PingMessage {
    const message = createBasePingMessage();
    message.clientTimeMs = object.clientTimeMs ?? "0";
    message.code = object.code ?? 0;
    return message;
  },
};

function createBasePongMessage(): PongMessage {
  return { clientTimeMs: "0", serverTimeMs: "0", code: 0 };
}

export const PongMessage: MessageFns<PongMessage> = {
  encode(message: PongMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientTimeMs !== "0") {
      writer.uint32(8).uint64(message.clientTimeMs);
    }
    if (message.serverTimeMs !== "0") {
      writer.uint32(16).uint64(message.serverTimeMs);
    }
    if (message.code !== 0) {
      writer.uint32(24).uint32(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PongMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePongMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.clientTimeMs = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serverTimeMs = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.code = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PongMessage {
    return {
      clientTimeMs: isSet(object.clientTimeMs)
        ? globalThis.String(object.clientTimeMs)
        : isSet(object.client_time_ms)
        ? globalThis.String(object.client_time_ms)
        : "0",
      serverTimeMs: isSet(object.serverTimeMs)
        ? globalThis.String(object.serverTimeMs)
        : isSet(object.server_time_ms)
        ? globalThis.String(object.server_time_ms)
        : "0",
      code: isSet(object.code) ? globalThis.Number(object.code) : 0,
    };
  },

  toJSON(message: PongMessage): unknown {
    const obj: any = {};
    if (message.clientTimeMs !== "0") {
      obj.clientTimeMs = message.clientTimeMs;
    }
    if (message.serverTimeMs !== "0") {
      obj.serverTimeMs = message.serverTimeMs;
    }
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PongMessage>, I>>(base?: I): PongMessage {
    return PongMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PongMessage>, I>>(object: I): PongMessage {
    const message = createBasePongMessage();
    message.clientTimeMs = object.clientTimeMs ?? "0";
    message.serverTimeMs = object.serverTimeMs ?? "0";
    message.code = object.code ?? 0;
    return message;
  },
};

function createBaseScpEnvelope(): ScpEnvelope {
  return { messageType: 0, messageBytes: new Uint8Array(0) };
}

export const ScpEnvelope: MessageFns<ScpEnvelope> = {
  encode(message: ScpEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messageType !== 0) {
      writer.uint32(8).int32(message.messageType);
    }
    if (message.messageBytes.length !== 0) {
      writer.uint32(18).bytes(message.messageBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScpEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScpEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.messageType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messageBytes = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScpEnvelope {
    return {
      messageType: isSet(object.messageType)
        ? messageTypeFromJSON(object.messageType)
        : isSet(object.message_type)
        ? messageTypeFromJSON(object.message_type)
        : 0,
      messageBytes: isSet(object.messageBytes)
        ? bytesFromBase64(object.messageBytes)
        : isSet(object.message_bytes)
        ? bytesFromBase64(object.message_bytes)
        : new Uint8Array(0),
    };
  },

  toJSON(message: ScpEnvelope): unknown {
    const obj: any = {};
    if (message.messageType !== 0) {
      obj.messageType = messageTypeToJSON(message.messageType);
    }
    if (message.messageBytes.length !== 0) {
      obj.messageBytes = base64FromBytes(message.messageBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScpEnvelope>, I>>(base?: I): ScpEnvelope {
    return ScpEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScpEnvelope>, I>>(object: I): ScpEnvelope {
    const message = createBaseScpEnvelope();
    message.messageType = object.messageType ?? 0;
    message.messageBytes = object.messageBytes ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
