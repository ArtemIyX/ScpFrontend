// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.36.0
// source: display_settings.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export enum DisplaySettingRequestType {
  FULLSCREEN_MODE = 0,
  RESOLUTION = 1,
  VSYNC = 2,
  FRAME_RATE_LIMIT = 3,
  HDR = 4,
  ACCESSIBILITY = 5,
  FIELD_OF_VIEW = 6,
  CAMERA = 7,
  UNRECOGNIZED = -1,
}

export function displaySettingRequestTypeFromJSON(object: any): DisplaySettingRequestType {
  switch (object) {
    case 0:
    case "FULLSCREEN_MODE":
      return DisplaySettingRequestType.FULLSCREEN_MODE;
    case 1:
    case "RESOLUTION":
      return DisplaySettingRequestType.RESOLUTION;
    case 2:
    case "VSYNC":
      return DisplaySettingRequestType.VSYNC;
    case 3:
    case "FRAME_RATE_LIMIT":
      return DisplaySettingRequestType.FRAME_RATE_LIMIT;
    case 4:
    case "HDR":
      return DisplaySettingRequestType.HDR;
    case 5:
    case "ACCESSIBILITY":
      return DisplaySettingRequestType.ACCESSIBILITY;
    case 6:
    case "FIELD_OF_VIEW":
      return DisplaySettingRequestType.FIELD_OF_VIEW;
    case 7:
    case "CAMERA":
      return DisplaySettingRequestType.CAMERA;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DisplaySettingRequestType.UNRECOGNIZED;
  }
}

export function displaySettingRequestTypeToJSON(object: DisplaySettingRequestType): string {
  switch (object) {
    case DisplaySettingRequestType.FULLSCREEN_MODE:
      return "FULLSCREEN_MODE";
    case DisplaySettingRequestType.RESOLUTION:
      return "RESOLUTION";
    case DisplaySettingRequestType.VSYNC:
      return "VSYNC";
    case DisplaySettingRequestType.FRAME_RATE_LIMIT:
      return "FRAME_RATE_LIMIT";
    case DisplaySettingRequestType.HDR:
      return "HDR";
    case DisplaySettingRequestType.ACCESSIBILITY:
      return "ACCESSIBILITY";
    case DisplaySettingRequestType.FIELD_OF_VIEW:
      return "FIELD_OF_VIEW";
    case DisplaySettingRequestType.CAMERA:
      return "CAMERA";
    case DisplaySettingRequestType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum FullscreenMode {
  FULLSCREEN = 0,
  WINDOWED_FULLSCREEN = 1,
  WINDOWED = 2,
  UNRECOGNIZED = -1,
}

export function fullscreenModeFromJSON(object: any): FullscreenMode {
  switch (object) {
    case 0:
    case "FULLSCREEN":
      return FullscreenMode.FULLSCREEN;
    case 1:
    case "WINDOWED_FULLSCREEN":
      return FullscreenMode.WINDOWED_FULLSCREEN;
    case 2:
    case "WINDOWED":
      return FullscreenMode.WINDOWED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FullscreenMode.UNRECOGNIZED;
  }
}

export function fullscreenModeToJSON(object: FullscreenMode): string {
  switch (object) {
    case FullscreenMode.FULLSCREEN:
      return "FULLSCREEN";
    case FullscreenMode.WINDOWED_FULLSCREEN:
      return "WINDOWED_FULLSCREEN";
    case FullscreenMode.WINDOWED:
      return "WINDOWED";
    case FullscreenMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum HdrOutput {
  SRGB = 0,
  REC = 1,
  LDR = 2,
  ACES_1000 = 3,
  ACES_2000 = 4,
  ACES_1000_ScRGB = 5,
  ACES_2000_ScRGB = 6,
  UNRECOGNIZED = -1,
}

export function hdrOutputFromJSON(object: any): HdrOutput {
  switch (object) {
    case 0:
    case "SRGB":
      return HdrOutput.SRGB;
    case 1:
    case "REC":
      return HdrOutput.REC;
    case 2:
    case "LDR":
      return HdrOutput.LDR;
    case 3:
    case "ACES_1000":
      return HdrOutput.ACES_1000;
    case 4:
    case "ACES_2000":
      return HdrOutput.ACES_2000;
    case 5:
    case "ACES_1000_ScRGB":
      return HdrOutput.ACES_1000_ScRGB;
    case 6:
    case "ACES_2000_ScRGB":
      return HdrOutput.ACES_2000_ScRGB;
    case -1:
    case "UNRECOGNIZED":
    default:
      return HdrOutput.UNRECOGNIZED;
  }
}

export function hdrOutputToJSON(object: HdrOutput): string {
  switch (object) {
    case HdrOutput.SRGB:
      return "SRGB";
    case HdrOutput.REC:
      return "REC";
    case HdrOutput.LDR:
      return "LDR";
    case HdrOutput.ACES_1000:
      return "ACES_1000";
    case HdrOutput.ACES_2000:
      return "ACES_2000";
    case HdrOutput.ACES_1000_ScRGB:
      return "ACES_1000_ScRGB";
    case HdrOutput.ACES_2000_ScRGB:
      return "ACES_2000_ScRGB";
    case HdrOutput.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ColorGamut {
  REC_709 = 0,
  DCI = 1,
  REC_2020 = 2,
  ACES = 3,
  ACESsg = 4,
  UNRECOGNIZED = -1,
}

export function colorGamutFromJSON(object: any): ColorGamut {
  switch (object) {
    case 0:
    case "REC_709":
      return ColorGamut.REC_709;
    case 1:
    case "DCI":
      return ColorGamut.DCI;
    case 2:
    case "REC_2020":
      return ColorGamut.REC_2020;
    case 3:
    case "ACES":
      return ColorGamut.ACES;
    case 4:
    case "ACESsg":
      return ColorGamut.ACESsg;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ColorGamut.UNRECOGNIZED;
  }
}

export function colorGamutToJSON(object: ColorGamut): string {
  switch (object) {
    case ColorGamut.REC_709:
      return "REC_709";
    case ColorGamut.DCI:
      return "DCI";
    case ColorGamut.REC_2020:
      return "REC_2020";
    case ColorGamut.ACES:
      return "ACES";
    case ColorGamut.ACESsg:
      return "ACESsg";
    case ColorGamut.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum DisplaySliderType {
  BRIGHTNESS = 0,
  GAMMA = 1,
  CONTRAST = 2,
  FOV = 3,
  CAMERA_SMOOTHING = 4,
  SCREEN_SHAKE_INTENSITY = 5,
  HEAD_BOBBING_INTENSITY = 6,
  UNRECOGNIZED = -1,
}

export function displaySliderTypeFromJSON(object: any): DisplaySliderType {
  switch (object) {
    case 0:
    case "BRIGHTNESS":
      return DisplaySliderType.BRIGHTNESS;
    case 1:
    case "GAMMA":
      return DisplaySliderType.GAMMA;
    case 2:
    case "CONTRAST":
      return DisplaySliderType.CONTRAST;
    case 3:
    case "FOV":
      return DisplaySliderType.FOV;
    case 4:
    case "CAMERA_SMOOTHING":
      return DisplaySliderType.CAMERA_SMOOTHING;
    case 5:
    case "SCREEN_SHAKE_INTENSITY":
      return DisplaySliderType.SCREEN_SHAKE_INTENSITY;
    case 6:
    case "HEAD_BOBBING_INTENSITY":
      return DisplaySliderType.HEAD_BOBBING_INTENSITY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DisplaySliderType.UNRECOGNIZED;
  }
}

export function displaySliderTypeToJSON(object: DisplaySliderType): string {
  switch (object) {
    case DisplaySliderType.BRIGHTNESS:
      return "BRIGHTNESS";
    case DisplaySliderType.GAMMA:
      return "GAMMA";
    case DisplaySliderType.CONTRAST:
      return "CONTRAST";
    case DisplaySliderType.FOV:
      return "FOV";
    case DisplaySliderType.CAMERA_SMOOTHING:
      return "CAMERA_SMOOTHING";
    case DisplaySliderType.SCREEN_SHAKE_INTENSITY:
      return "SCREEN_SHAKE_INTENSITY";
    case DisplaySliderType.HEAD_BOBBING_INTENSITY:
      return "HEAD_BOBBING_INTENSITY";
    case DisplaySliderType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface DisplaySettingsRequest {
  requestType: DisplaySettingRequestType;
}

export interface Resolution {
  x: number;
  y: number;
}

export interface ResolutionResponse {
  resolutions: Resolution[];
}

export interface VSyncContent {
  enabled: boolean;
}

export interface FrameLimitContent {
  flag: boolean;
  limit: number;
}

export interface HdrResponse {
  supported: boolean;
  enabled: boolean;
  outputDevice: HdrOutput;
  colorGamut: ColorGamut;
}

export interface AccessibilityResponse {
  brightness: number;
  gamma: number;
  contrast: number;
}

export interface CameraResponse {
  fov: number;
  smoothing: number;
  shake: number;
  bob: number;
}

export interface ResolutionSetRequest {
  resolution: Resolution | undefined;
}

export interface HdrEnableSetRequest {
  flag: boolean;
}

export interface HdrOutputDeviceSetRequest {
  outputDevice: HdrOutput;
}

export interface HdrColorGamutSetRequest {
  colorGamut: ColorGamut;
}

export interface DisplaySliderSetRequest {
  sliderType: DisplaySliderType;
  value: number;
}

function createBaseDisplaySettingsRequest(): DisplaySettingsRequest {
  return { requestType: 0 };
}

export const DisplaySettingsRequest: MessageFns<DisplaySettingsRequest> = {
  encode(message: DisplaySettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestType !== 0) {
      writer.uint32(8).int32(message.requestType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DisplaySettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisplaySettingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requestType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DisplaySettingsRequest {
    return {
      requestType: isSet(object.requestType)
        ? displaySettingRequestTypeFromJSON(object.requestType)
        : isSet(object.request_type)
        ? displaySettingRequestTypeFromJSON(object.request_type)
        : 0,
    };
  },

  toJSON(message: DisplaySettingsRequest): unknown {
    const obj: any = {};
    if (message.requestType !== 0) {
      obj.requestType = displaySettingRequestTypeToJSON(message.requestType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DisplaySettingsRequest>, I>>(base?: I): DisplaySettingsRequest {
    return DisplaySettingsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DisplaySettingsRequest>, I>>(object: I): DisplaySettingsRequest {
    const message = createBaseDisplaySettingsRequest();
    message.requestType = object.requestType ?? 0;
    return message;
  },
};

function createBaseResolution(): Resolution {
  return { x: 0, y: 0 };
}

export const Resolution: MessageFns<Resolution> = {
  encode(message: Resolution, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(8).uint32(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(16).uint32(message.y);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Resolution {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.x = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.y = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Resolution {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
    };
  },

  toJSON(message: Resolution): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = Math.round(message.x);
    }
    if (message.y !== 0) {
      obj.y = Math.round(message.y);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Resolution>, I>>(base?: I): Resolution {
    return Resolution.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Resolution>, I>>(object: I): Resolution {
    const message = createBaseResolution();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    return message;
  },
};

function createBaseResolutionResponse(): ResolutionResponse {
  return { resolutions: [] };
}

export const ResolutionResponse: MessageFns<ResolutionResponse> = {
  encode(message: ResolutionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.resolutions) {
      Resolution.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolutionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolutionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resolutions.push(Resolution.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolutionResponse {
    return {
      resolutions: globalThis.Array.isArray(object?.resolutions)
        ? object.resolutions.map((e: any) => Resolution.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ResolutionResponse): unknown {
    const obj: any = {};
    if (message.resolutions?.length) {
      obj.resolutions = message.resolutions.map((e) => Resolution.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResolutionResponse>, I>>(base?: I): ResolutionResponse {
    return ResolutionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolutionResponse>, I>>(object: I): ResolutionResponse {
    const message = createBaseResolutionResponse();
    message.resolutions = object.resolutions?.map((e) => Resolution.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVSyncContent(): VSyncContent {
  return { enabled: false };
}

export const VSyncContent: MessageFns<VSyncContent> = {
  encode(message: VSyncContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VSyncContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVSyncContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VSyncContent {
    return { enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false };
  },

  toJSON(message: VSyncContent): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VSyncContent>, I>>(base?: I): VSyncContent {
    return VSyncContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VSyncContent>, I>>(object: I): VSyncContent {
    const message = createBaseVSyncContent();
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseFrameLimitContent(): FrameLimitContent {
  return { flag: false, limit: 0 };
}

export const FrameLimitContent: MessageFns<FrameLimitContent> = {
  encode(message: FrameLimitContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flag !== false) {
      writer.uint32(8).bool(message.flag);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FrameLimitContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFrameLimitContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.flag = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FrameLimitContent {
    return {
      flag: isSet(object.flag) ? globalThis.Boolean(object.flag) : false,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: FrameLimitContent): unknown {
    const obj: any = {};
    if (message.flag !== false) {
      obj.flag = message.flag;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FrameLimitContent>, I>>(base?: I): FrameLimitContent {
    return FrameLimitContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FrameLimitContent>, I>>(object: I): FrameLimitContent {
    const message = createBaseFrameLimitContent();
    message.flag = object.flag ?? false;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseHdrResponse(): HdrResponse {
  return { supported: false, enabled: false, outputDevice: 0, colorGamut: 0 };
}

export const HdrResponse: MessageFns<HdrResponse> = {
  encode(message: HdrResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.supported !== false) {
      writer.uint32(8).bool(message.supported);
    }
    if (message.enabled !== false) {
      writer.uint32(16).bool(message.enabled);
    }
    if (message.outputDevice !== 0) {
      writer.uint32(24).int32(message.outputDevice);
    }
    if (message.colorGamut !== 0) {
      writer.uint32(32).int32(message.colorGamut);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HdrResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHdrResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.supported = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.outputDevice = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.colorGamut = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HdrResponse {
    return {
      supported: isSet(object.supported) ? globalThis.Boolean(object.supported) : false,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      outputDevice: isSet(object.outputDevice)
        ? hdrOutputFromJSON(object.outputDevice)
        : isSet(object.output_device)
        ? hdrOutputFromJSON(object.output_device)
        : 0,
      colorGamut: isSet(object.colorGamut)
        ? colorGamutFromJSON(object.colorGamut)
        : isSet(object.color_gamut)
        ? colorGamutFromJSON(object.color_gamut)
        : 0,
    };
  },

  toJSON(message: HdrResponse): unknown {
    const obj: any = {};
    if (message.supported !== false) {
      obj.supported = message.supported;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.outputDevice !== 0) {
      obj.outputDevice = hdrOutputToJSON(message.outputDevice);
    }
    if (message.colorGamut !== 0) {
      obj.colorGamut = colorGamutToJSON(message.colorGamut);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HdrResponse>, I>>(base?: I): HdrResponse {
    return HdrResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HdrResponse>, I>>(object: I): HdrResponse {
    const message = createBaseHdrResponse();
    message.supported = object.supported ?? false;
    message.enabled = object.enabled ?? false;
    message.outputDevice = object.outputDevice ?? 0;
    message.colorGamut = object.colorGamut ?? 0;
    return message;
  },
};

function createBaseAccessibilityResponse(): AccessibilityResponse {
  return { brightness: 0, gamma: 0, contrast: 0 };
}

export const AccessibilityResponse: MessageFns<AccessibilityResponse> = {
  encode(message: AccessibilityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.brightness !== 0) {
      writer.uint32(8).uint32(message.brightness);
    }
    if (message.gamma !== 0) {
      writer.uint32(16).uint32(message.gamma);
    }
    if (message.contrast !== 0) {
      writer.uint32(24).uint32(message.contrast);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccessibilityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccessibilityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.brightness = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.gamma = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.contrast = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccessibilityResponse {
    return {
      brightness: isSet(object.brightness) ? globalThis.Number(object.brightness) : 0,
      gamma: isSet(object.gamma) ? globalThis.Number(object.gamma) : 0,
      contrast: isSet(object.contrast) ? globalThis.Number(object.contrast) : 0,
    };
  },

  toJSON(message: AccessibilityResponse): unknown {
    const obj: any = {};
    if (message.brightness !== 0) {
      obj.brightness = Math.round(message.brightness);
    }
    if (message.gamma !== 0) {
      obj.gamma = Math.round(message.gamma);
    }
    if (message.contrast !== 0) {
      obj.contrast = Math.round(message.contrast);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccessibilityResponse>, I>>(base?: I): AccessibilityResponse {
    return AccessibilityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccessibilityResponse>, I>>(object: I): AccessibilityResponse {
    const message = createBaseAccessibilityResponse();
    message.brightness = object.brightness ?? 0;
    message.gamma = object.gamma ?? 0;
    message.contrast = object.contrast ?? 0;
    return message;
  },
};

function createBaseCameraResponse(): CameraResponse {
  return { fov: 0, smoothing: 0, shake: 0, bob: 0 };
}

export const CameraResponse: MessageFns<CameraResponse> = {
  encode(message: CameraResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fov !== 0) {
      writer.uint32(8).uint32(message.fov);
    }
    if (message.smoothing !== 0) {
      writer.uint32(16).uint32(message.smoothing);
    }
    if (message.shake !== 0) {
      writer.uint32(24).uint32(message.shake);
    }
    if (message.bob !== 0) {
      writer.uint32(32).uint32(message.bob);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CameraResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCameraResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fov = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.smoothing = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.shake = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.bob = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CameraResponse {
    return {
      fov: isSet(object.fov) ? globalThis.Number(object.fov) : 0,
      smoothing: isSet(object.smoothing) ? globalThis.Number(object.smoothing) : 0,
      shake: isSet(object.shake) ? globalThis.Number(object.shake) : 0,
      bob: isSet(object.bob) ? globalThis.Number(object.bob) : 0,
    };
  },

  toJSON(message: CameraResponse): unknown {
    const obj: any = {};
    if (message.fov !== 0) {
      obj.fov = Math.round(message.fov);
    }
    if (message.smoothing !== 0) {
      obj.smoothing = Math.round(message.smoothing);
    }
    if (message.shake !== 0) {
      obj.shake = Math.round(message.shake);
    }
    if (message.bob !== 0) {
      obj.bob = Math.round(message.bob);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CameraResponse>, I>>(base?: I): CameraResponse {
    return CameraResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CameraResponse>, I>>(object: I): CameraResponse {
    const message = createBaseCameraResponse();
    message.fov = object.fov ?? 0;
    message.smoothing = object.smoothing ?? 0;
    message.shake = object.shake ?? 0;
    message.bob = object.bob ?? 0;
    return message;
  },
};

function createBaseResolutionSetRequest(): ResolutionSetRequest {
  return { resolution: undefined };
}

export const ResolutionSetRequest: MessageFns<ResolutionSetRequest> = {
  encode(message: ResolutionSetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resolution !== undefined) {
      Resolution.encode(message.resolution, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolutionSetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolutionSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resolution = Resolution.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolutionSetRequest {
    return { resolution: isSet(object.resolution) ? Resolution.fromJSON(object.resolution) : undefined };
  },

  toJSON(message: ResolutionSetRequest): unknown {
    const obj: any = {};
    if (message.resolution !== undefined) {
      obj.resolution = Resolution.toJSON(message.resolution);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResolutionSetRequest>, I>>(base?: I): ResolutionSetRequest {
    return ResolutionSetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolutionSetRequest>, I>>(object: I): ResolutionSetRequest {
    const message = createBaseResolutionSetRequest();
    message.resolution = (object.resolution !== undefined && object.resolution !== null)
      ? Resolution.fromPartial(object.resolution)
      : undefined;
    return message;
  },
};

function createBaseHdrEnableSetRequest(): HdrEnableSetRequest {
  return { flag: false };
}

export const HdrEnableSetRequest: MessageFns<HdrEnableSetRequest> = {
  encode(message: HdrEnableSetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flag !== false) {
      writer.uint32(8).bool(message.flag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HdrEnableSetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHdrEnableSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.flag = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HdrEnableSetRequest {
    return { flag: isSet(object.flag) ? globalThis.Boolean(object.flag) : false };
  },

  toJSON(message: HdrEnableSetRequest): unknown {
    const obj: any = {};
    if (message.flag !== false) {
      obj.flag = message.flag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HdrEnableSetRequest>, I>>(base?: I): HdrEnableSetRequest {
    return HdrEnableSetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HdrEnableSetRequest>, I>>(object: I): HdrEnableSetRequest {
    const message = createBaseHdrEnableSetRequest();
    message.flag = object.flag ?? false;
    return message;
  },
};

function createBaseHdrOutputDeviceSetRequest(): HdrOutputDeviceSetRequest {
  return { outputDevice: 0 };
}

export const HdrOutputDeviceSetRequest: MessageFns<HdrOutputDeviceSetRequest> = {
  encode(message: HdrOutputDeviceSetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.outputDevice !== 0) {
      writer.uint32(8).int32(message.outputDevice);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HdrOutputDeviceSetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHdrOutputDeviceSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.outputDevice = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HdrOutputDeviceSetRequest {
    return {
      outputDevice: isSet(object.outputDevice)
        ? hdrOutputFromJSON(object.outputDevice)
        : isSet(object.output_device)
        ? hdrOutputFromJSON(object.output_device)
        : 0,
    };
  },

  toJSON(message: HdrOutputDeviceSetRequest): unknown {
    const obj: any = {};
    if (message.outputDevice !== 0) {
      obj.outputDevice = hdrOutputToJSON(message.outputDevice);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HdrOutputDeviceSetRequest>, I>>(base?: I): HdrOutputDeviceSetRequest {
    return HdrOutputDeviceSetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HdrOutputDeviceSetRequest>, I>>(object: I): HdrOutputDeviceSetRequest {
    const message = createBaseHdrOutputDeviceSetRequest();
    message.outputDevice = object.outputDevice ?? 0;
    return message;
  },
};

function createBaseHdrColorGamutSetRequest(): HdrColorGamutSetRequest {
  return { colorGamut: 0 };
}

export const HdrColorGamutSetRequest: MessageFns<HdrColorGamutSetRequest> = {
  encode(message: HdrColorGamutSetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.colorGamut !== 0) {
      writer.uint32(8).int32(message.colorGamut);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HdrColorGamutSetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHdrColorGamutSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.colorGamut = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HdrColorGamutSetRequest {
    return {
      colorGamut: isSet(object.colorGamut)
        ? colorGamutFromJSON(object.colorGamut)
        : isSet(object.color_gamut)
        ? colorGamutFromJSON(object.color_gamut)
        : 0,
    };
  },

  toJSON(message: HdrColorGamutSetRequest): unknown {
    const obj: any = {};
    if (message.colorGamut !== 0) {
      obj.colorGamut = colorGamutToJSON(message.colorGamut);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HdrColorGamutSetRequest>, I>>(base?: I): HdrColorGamutSetRequest {
    return HdrColorGamutSetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HdrColorGamutSetRequest>, I>>(object: I): HdrColorGamutSetRequest {
    const message = createBaseHdrColorGamutSetRequest();
    message.colorGamut = object.colorGamut ?? 0;
    return message;
  },
};

function createBaseDisplaySliderSetRequest(): DisplaySliderSetRequest {
  return { sliderType: 0, value: 0 };
}

export const DisplaySliderSetRequest: MessageFns<DisplaySliderSetRequest> = {
  encode(message: DisplaySliderSetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sliderType !== 0) {
      writer.uint32(8).int32(message.sliderType);
    }
    if (message.value !== 0) {
      writer.uint32(16).uint32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DisplaySliderSetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisplaySliderSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sliderType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DisplaySliderSetRequest {
    return {
      sliderType: isSet(object.sliderType)
        ? displaySliderTypeFromJSON(object.sliderType)
        : isSet(object.slider_type)
        ? displaySliderTypeFromJSON(object.slider_type)
        : 0,
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: DisplaySliderSetRequest): unknown {
    const obj: any = {};
    if (message.sliderType !== 0) {
      obj.sliderType = displaySliderTypeToJSON(message.sliderType);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DisplaySliderSetRequest>, I>>(base?: I): DisplaySliderSetRequest {
    return DisplaySliderSetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DisplaySliderSetRequest>, I>>(object: I): DisplaySliderSetRequest {
    const message = createBaseDisplaySliderSetRequest();
    message.sliderType = object.sliderType ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
