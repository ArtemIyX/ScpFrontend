// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.36.0
// source: keybings_settings.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface KeyBindingSettingVisualData {
  uniqueId: string;
  displayKey: string;
  key: string;
}

export interface KeyBindingSettingCategory {
  categoryName: string;
  keyBindings: KeyBindingSettingVisualData[];
}

/** Retrieve all categories with display_key for localization */
export interface KeyBindingsSettingsResponse {
  categories: KeyBindingSettingCategory[];
}

export interface KeyBindingSettingData {
  uniqueId: string;
  key: string;
}

/** Set key_bindings -> new values */
export interface KeyBindingsSettingsSetRequest {
  keyBindings: KeyBindingSettingData[];
}

function createBaseKeyBindingSettingVisualData(): KeyBindingSettingVisualData {
  return { uniqueId: "", displayKey: "", key: "" };
}

export const KeyBindingSettingVisualData: MessageFns<KeyBindingSettingVisualData> = {
  encode(message: KeyBindingSettingVisualData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uniqueId !== "") {
      writer.uint32(10).string(message.uniqueId);
    }
    if (message.displayKey !== "") {
      writer.uint32(18).string(message.displayKey);
    }
    if (message.key !== "") {
      writer.uint32(26).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyBindingSettingVisualData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyBindingSettingVisualData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uniqueId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyBindingSettingVisualData {
    return {
      uniqueId: isSet(object.uniqueId)
        ? globalThis.String(object.uniqueId)
        : isSet(object.unique_id)
        ? globalThis.String(object.unique_id)
        : "",
      displayKey: isSet(object.displayKey)
        ? globalThis.String(object.displayKey)
        : isSet(object.display_key)
        ? globalThis.String(object.display_key)
        : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
    };
  },

  toJSON(message: KeyBindingSettingVisualData): unknown {
    const obj: any = {};
    if (message.uniqueId !== "") {
      obj.uniqueId = message.uniqueId;
    }
    if (message.displayKey !== "") {
      obj.displayKey = message.displayKey;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyBindingSettingVisualData>, I>>(base?: I): KeyBindingSettingVisualData {
    return KeyBindingSettingVisualData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyBindingSettingVisualData>, I>>(object: I): KeyBindingSettingVisualData {
    const message = createBaseKeyBindingSettingVisualData();
    message.uniqueId = object.uniqueId ?? "";
    message.displayKey = object.displayKey ?? "";
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseKeyBindingSettingCategory(): KeyBindingSettingCategory {
  return { categoryName: "", keyBindings: [] };
}

export const KeyBindingSettingCategory: MessageFns<KeyBindingSettingCategory> = {
  encode(message: KeyBindingSettingCategory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.categoryName !== "") {
      writer.uint32(10).string(message.categoryName);
    }
    for (const v of message.keyBindings) {
      KeyBindingSettingVisualData.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyBindingSettingCategory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyBindingSettingCategory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.categoryName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.keyBindings.push(KeyBindingSettingVisualData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyBindingSettingCategory {
    return {
      categoryName: isSet(object.categoryName)
        ? globalThis.String(object.categoryName)
        : isSet(object.category_name)
        ? globalThis.String(object.category_name)
        : "",
      keyBindings: globalThis.Array.isArray(object?.keyBindings)
        ? object.keyBindings.map((e: any) => KeyBindingSettingVisualData.fromJSON(e))
        : globalThis.Array.isArray(object?.key_bindings)
        ? object.key_bindings.map((e: any) => KeyBindingSettingVisualData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: KeyBindingSettingCategory): unknown {
    const obj: any = {};
    if (message.categoryName !== "") {
      obj.categoryName = message.categoryName;
    }
    if (message.keyBindings?.length) {
      obj.keyBindings = message.keyBindings.map((e) => KeyBindingSettingVisualData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyBindingSettingCategory>, I>>(base?: I): KeyBindingSettingCategory {
    return KeyBindingSettingCategory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyBindingSettingCategory>, I>>(object: I): KeyBindingSettingCategory {
    const message = createBaseKeyBindingSettingCategory();
    message.categoryName = object.categoryName ?? "";
    message.keyBindings = object.keyBindings?.map((e) => KeyBindingSettingVisualData.fromPartial(e)) || [];
    return message;
  },
};

function createBaseKeyBindingsSettingsResponse(): KeyBindingsSettingsResponse {
  return { categories: [] };
}

export const KeyBindingsSettingsResponse: MessageFns<KeyBindingsSettingsResponse> = {
  encode(message: KeyBindingsSettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.categories) {
      KeyBindingSettingCategory.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyBindingsSettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyBindingsSettingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.categories.push(KeyBindingSettingCategory.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyBindingsSettingsResponse {
    return {
      categories: globalThis.Array.isArray(object?.categories)
        ? object.categories.map((e: any) => KeyBindingSettingCategory.fromJSON(e))
        : [],
    };
  },

  toJSON(message: KeyBindingsSettingsResponse): unknown {
    const obj: any = {};
    if (message.categories?.length) {
      obj.categories = message.categories.map((e) => KeyBindingSettingCategory.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyBindingsSettingsResponse>, I>>(base?: I): KeyBindingsSettingsResponse {
    return KeyBindingsSettingsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyBindingsSettingsResponse>, I>>(object: I): KeyBindingsSettingsResponse {
    const message = createBaseKeyBindingsSettingsResponse();
    message.categories = object.categories?.map((e) => KeyBindingSettingCategory.fromPartial(e)) || [];
    return message;
  },
};

function createBaseKeyBindingSettingData(): KeyBindingSettingData {
  return { uniqueId: "", key: "" };
}

export const KeyBindingSettingData: MessageFns<KeyBindingSettingData> = {
  encode(message: KeyBindingSettingData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uniqueId !== "") {
      writer.uint32(10).string(message.uniqueId);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyBindingSettingData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyBindingSettingData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uniqueId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyBindingSettingData {
    return {
      uniqueId: isSet(object.uniqueId)
        ? globalThis.String(object.uniqueId)
        : isSet(object.unique_id)
        ? globalThis.String(object.unique_id)
        : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
    };
  },

  toJSON(message: KeyBindingSettingData): unknown {
    const obj: any = {};
    if (message.uniqueId !== "") {
      obj.uniqueId = message.uniqueId;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyBindingSettingData>, I>>(base?: I): KeyBindingSettingData {
    return KeyBindingSettingData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyBindingSettingData>, I>>(object: I): KeyBindingSettingData {
    const message = createBaseKeyBindingSettingData();
    message.uniqueId = object.uniqueId ?? "";
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseKeyBindingsSettingsSetRequest(): KeyBindingsSettingsSetRequest {
  return { keyBindings: [] };
}

export const KeyBindingsSettingsSetRequest: MessageFns<KeyBindingsSettingsSetRequest> = {
  encode(message: KeyBindingsSettingsSetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keyBindings) {
      KeyBindingSettingData.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyBindingsSettingsSetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyBindingsSettingsSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keyBindings.push(KeyBindingSettingData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyBindingsSettingsSetRequest {
    return {
      keyBindings: globalThis.Array.isArray(object?.keyBindings)
        ? object.keyBindings.map((e: any) => KeyBindingSettingData.fromJSON(e))
        : globalThis.Array.isArray(object?.key_bindings)
        ? object.key_bindings.map((e: any) => KeyBindingSettingData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: KeyBindingsSettingsSetRequest): unknown {
    const obj: any = {};
    if (message.keyBindings?.length) {
      obj.keyBindings = message.keyBindings.map((e) => KeyBindingSettingData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyBindingsSettingsSetRequest>, I>>(base?: I): KeyBindingsSettingsSetRequest {
    return KeyBindingsSettingsSetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyBindingsSettingsSetRequest>, I>>(
    object: I,
  ): KeyBindingsSettingsSetRequest {
    const message = createBaseKeyBindingsSettingsSetRequest();
    message.keyBindings = object.keyBindings?.map((e) => KeyBindingSettingData.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
