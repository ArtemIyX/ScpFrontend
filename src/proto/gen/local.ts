// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: local.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "scp.webui.v1";

export interface LocalizationRequestData {
  table: string;
  key: string;
}

export interface LocalizationResponseData {
  request: LocalizationRequestData | undefined;
  response: string;
}

export interface RequestGetLocalization {
  payload: LocalizationRequestData[];
}

export interface ResponseLocalization {
  payload: LocalizationResponseData[];
}

export interface RequestGetCurrentCulture {
  empty: number;
}

export interface RequestGetSupportedCultures {
  empty: number;
}

export interface ResponseCurrentCulture {
  cultureCode: string;
}

export interface CultureData {
  code: string;
  name: string;
}

export interface ResponseSupportedCultures {
  cultures: CultureData[];
}

export interface MessageCultureChanged {
  payload: CultureData | undefined;
}

export interface RequestSetCulture {
  code: string;
}

function createBaseLocalizationRequestData(): LocalizationRequestData {
  return { table: "", key: "" };
}

export const LocalizationRequestData: MessageFns<LocalizationRequestData> = {
  encode(message: LocalizationRequestData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.table !== "") {
      writer.uint32(10).string(message.table);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LocalizationRequestData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocalizationRequestData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.table = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LocalizationRequestData {
    return {
      table: isSet(object.table) ? globalThis.String(object.table) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
    };
  },

  toJSON(message: LocalizationRequestData): unknown {
    const obj: any = {};
    if (message.table !== "") {
      obj.table = message.table;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LocalizationRequestData>, I>>(base?: I): LocalizationRequestData {
    return LocalizationRequestData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LocalizationRequestData>, I>>(object: I): LocalizationRequestData {
    const message = createBaseLocalizationRequestData();
    message.table = object.table ?? "";
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseLocalizationResponseData(): LocalizationResponseData {
  return { request: undefined, response: "" };
}

export const LocalizationResponseData: MessageFns<LocalizationResponseData> = {
  encode(message: LocalizationResponseData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.request !== undefined) {
      LocalizationRequestData.encode(message.request, writer.uint32(10).fork()).join();
    }
    if (message.response !== "") {
      writer.uint32(18).string(message.response);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LocalizationResponseData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocalizationResponseData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.request = LocalizationRequestData.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.response = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LocalizationResponseData {
    return {
      request: isSet(object.request) ? LocalizationRequestData.fromJSON(object.request) : undefined,
      response: isSet(object.response) ? globalThis.String(object.response) : "",
    };
  },

  toJSON(message: LocalizationResponseData): unknown {
    const obj: any = {};
    if (message.request !== undefined) {
      obj.request = LocalizationRequestData.toJSON(message.request);
    }
    if (message.response !== "") {
      obj.response = message.response;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LocalizationResponseData>, I>>(base?: I): LocalizationResponseData {
    return LocalizationResponseData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LocalizationResponseData>, I>>(object: I): LocalizationResponseData {
    const message = createBaseLocalizationResponseData();
    message.request = (object.request !== undefined && object.request !== null)
      ? LocalizationRequestData.fromPartial(object.request)
      : undefined;
    message.response = object.response ?? "";
    return message;
  },
};

function createBaseRequestGetLocalization(): RequestGetLocalization {
  return { payload: [] };
}

export const RequestGetLocalization: MessageFns<RequestGetLocalization> = {
  encode(message: RequestGetLocalization, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.payload) {
      LocalizationRequestData.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestGetLocalization {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestGetLocalization();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload.push(LocalizationRequestData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestGetLocalization {
    return {
      payload: globalThis.Array.isArray(object?.payload)
        ? object.payload.map((e: any) => LocalizationRequestData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RequestGetLocalization): unknown {
    const obj: any = {};
    if (message.payload?.length) {
      obj.payload = message.payload.map((e) => LocalizationRequestData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestGetLocalization>, I>>(base?: I): RequestGetLocalization {
    return RequestGetLocalization.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestGetLocalization>, I>>(object: I): RequestGetLocalization {
    const message = createBaseRequestGetLocalization();
    message.payload = object.payload?.map((e) => LocalizationRequestData.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResponseLocalization(): ResponseLocalization {
  return { payload: [] };
}

export const ResponseLocalization: MessageFns<ResponseLocalization> = {
  encode(message: ResponseLocalization, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.payload) {
      LocalizationResponseData.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseLocalization {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseLocalization();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload.push(LocalizationResponseData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResponseLocalization {
    return {
      payload: globalThis.Array.isArray(object?.payload)
        ? object.payload.map((e: any) => LocalizationResponseData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ResponseLocalization): unknown {
    const obj: any = {};
    if (message.payload?.length) {
      obj.payload = message.payload.map((e) => LocalizationResponseData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseLocalization>, I>>(base?: I): ResponseLocalization {
    return ResponseLocalization.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseLocalization>, I>>(object: I): ResponseLocalization {
    const message = createBaseResponseLocalization();
    message.payload = object.payload?.map((e) => LocalizationResponseData.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRequestGetCurrentCulture(): RequestGetCurrentCulture {
  return { empty: 0 };
}

export const RequestGetCurrentCulture: MessageFns<RequestGetCurrentCulture> = {
  encode(message: RequestGetCurrentCulture, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.empty !== 0) {
      writer.uint32(8).uint32(message.empty);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestGetCurrentCulture {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestGetCurrentCulture();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.empty = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestGetCurrentCulture {
    return { empty: isSet(object.empty) ? globalThis.Number(object.empty) : 0 };
  },

  toJSON(message: RequestGetCurrentCulture): unknown {
    const obj: any = {};
    if (message.empty !== 0) {
      obj.empty = Math.round(message.empty);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestGetCurrentCulture>, I>>(base?: I): RequestGetCurrentCulture {
    return RequestGetCurrentCulture.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestGetCurrentCulture>, I>>(object: I): RequestGetCurrentCulture {
    const message = createBaseRequestGetCurrentCulture();
    message.empty = object.empty ?? 0;
    return message;
  },
};

function createBaseRequestGetSupportedCultures(): RequestGetSupportedCultures {
  return { empty: 0 };
}

export const RequestGetSupportedCultures: MessageFns<RequestGetSupportedCultures> = {
  encode(message: RequestGetSupportedCultures, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.empty !== 0) {
      writer.uint32(8).uint32(message.empty);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestGetSupportedCultures {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestGetSupportedCultures();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.empty = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestGetSupportedCultures {
    return { empty: isSet(object.empty) ? globalThis.Number(object.empty) : 0 };
  },

  toJSON(message: RequestGetSupportedCultures): unknown {
    const obj: any = {};
    if (message.empty !== 0) {
      obj.empty = Math.round(message.empty);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestGetSupportedCultures>, I>>(base?: I): RequestGetSupportedCultures {
    return RequestGetSupportedCultures.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestGetSupportedCultures>, I>>(object: I): RequestGetSupportedCultures {
    const message = createBaseRequestGetSupportedCultures();
    message.empty = object.empty ?? 0;
    return message;
  },
};

function createBaseResponseCurrentCulture(): ResponseCurrentCulture {
  return { cultureCode: "" };
}

export const ResponseCurrentCulture: MessageFns<ResponseCurrentCulture> = {
  encode(message: ResponseCurrentCulture, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cultureCode !== "") {
      writer.uint32(10).string(message.cultureCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseCurrentCulture {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseCurrentCulture();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cultureCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResponseCurrentCulture {
    return {
      cultureCode: isSet(object.cultureCode)
        ? globalThis.String(object.cultureCode)
        : isSet(object.culture_code)
        ? globalThis.String(object.culture_code)
        : "",
    };
  },

  toJSON(message: ResponseCurrentCulture): unknown {
    const obj: any = {};
    if (message.cultureCode !== "") {
      obj.cultureCode = message.cultureCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseCurrentCulture>, I>>(base?: I): ResponseCurrentCulture {
    return ResponseCurrentCulture.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseCurrentCulture>, I>>(object: I): ResponseCurrentCulture {
    const message = createBaseResponseCurrentCulture();
    message.cultureCode = object.cultureCode ?? "";
    return message;
  },
};

function createBaseCultureData(): CultureData {
  return { code: "", name: "" };
}

export const CultureData: MessageFns<CultureData> = {
  encode(message: CultureData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CultureData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCultureData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CultureData {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: CultureData): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CultureData>, I>>(base?: I): CultureData {
    return CultureData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CultureData>, I>>(object: I): CultureData {
    const message = createBaseCultureData();
    message.code = object.code ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseResponseSupportedCultures(): ResponseSupportedCultures {
  return { cultures: [] };
}

export const ResponseSupportedCultures: MessageFns<ResponseSupportedCultures> = {
  encode(message: ResponseSupportedCultures, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.cultures) {
      CultureData.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseSupportedCultures {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseSupportedCultures();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cultures.push(CultureData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResponseSupportedCultures {
    return {
      cultures: globalThis.Array.isArray(object?.cultures)
        ? object.cultures.map((e: any) => CultureData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ResponseSupportedCultures): unknown {
    const obj: any = {};
    if (message.cultures?.length) {
      obj.cultures = message.cultures.map((e) => CultureData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseSupportedCultures>, I>>(base?: I): ResponseSupportedCultures {
    return ResponseSupportedCultures.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseSupportedCultures>, I>>(object: I): ResponseSupportedCultures {
    const message = createBaseResponseSupportedCultures();
    message.cultures = object.cultures?.map((e) => CultureData.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMessageCultureChanged(): MessageCultureChanged {
  return { payload: undefined };
}

export const MessageCultureChanged: MessageFns<MessageCultureChanged> = {
  encode(message: MessageCultureChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload !== undefined) {
      CultureData.encode(message.payload, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageCultureChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageCultureChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = CultureData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageCultureChanged {
    return { payload: isSet(object.payload) ? CultureData.fromJSON(object.payload) : undefined };
  },

  toJSON(message: MessageCultureChanged): unknown {
    const obj: any = {};
    if (message.payload !== undefined) {
      obj.payload = CultureData.toJSON(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageCultureChanged>, I>>(base?: I): MessageCultureChanged {
    return MessageCultureChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageCultureChanged>, I>>(object: I): MessageCultureChanged {
    const message = createBaseMessageCultureChanged();
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? CultureData.fromPartial(object.payload)
      : undefined;
    return message;
  },
};

function createBaseRequestSetCulture(): RequestSetCulture {
  return { code: "" };
}

export const RequestSetCulture: MessageFns<RequestSetCulture> = {
  encode(message: RequestSetCulture, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestSetCulture {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestSetCulture();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestSetCulture {
    return { code: isSet(object.code) ? globalThis.String(object.code) : "" };
  },

  toJSON(message: RequestSetCulture): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestSetCulture>, I>>(base?: I): RequestSetCulture {
    return RequestSetCulture.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestSetCulture>, I>>(object: I): RequestSetCulture {
    const message = createBaseRequestSetCulture();
    message.code = object.code ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
